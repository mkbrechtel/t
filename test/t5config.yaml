# Test configuration for t5
todo:
  files:
    - path: "./todo.txt"
      ensure:
        prefershortids: true
        enforcecompletiondate: true
    - path: "./work.txt"
      ensure:
        prefershortids: true
        enforcecompletiondate: true
    
eventstore:
  file: "./events.jsonl"
